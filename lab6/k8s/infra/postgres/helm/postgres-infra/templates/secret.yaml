apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  labels:
    {{- include "postgres-infra.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_DB: {{ .Values.database.name | quote }}
  POSTGRES_USER: {{ .Values.database.user | quote }}
  POSTGRES_PASSWORD: {{ .Values.database.password | quote }}
